library(shiny)

ui <- fluidPage(
  titlePanel("Tree Split Demo (Placeholder)"),
  p("TODO: Add split criterion controls and partition visualization.")
)

server <- function(input, output, session) {}

shinyApp(ui, server)
